import 'package:flutter/material.dart';

class AddCuidado extends StatelessWidget {
  const AddCuidado({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}